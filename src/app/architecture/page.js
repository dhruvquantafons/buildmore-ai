import ArchitectureClient from './ArchitectureClient';

export const metadata = {
  title: 'System Architecture',
  description: 'The five-tier BuildMore AI architecture, the module interconnection map, the 48-month development roadmap, revenue architecture, addressable market and the full technology stack.',
};

export default function ArchitecturePage() {
  return <ArchitectureClient />;
}
